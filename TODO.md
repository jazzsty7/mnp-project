# TODO

## 진행

- 채팅방 목록과 실제 저장된 방 데이터를 더 강하게 동기화한다.
- 친구 추가, 수정, 삭제 흐름을 하나의 공통 편집 화면으로 더 정리한다.
- 채팅 메시지 저장 구조를 더 명확하게 정리하고, 예전 더미 데이터와의 호환성을 마무리한다.
- 홈 화면의 친구 수, 채팅방 수, 최근 상태 표시를 모두 실제 저장값 기준으로 유지한다.

## 검증

- `home.html`, `chat_list.html`, `chat_room.html`, `friends.html`의 화면별 렌더가 새로고침 후에도 정상 유지되는지 확인한다.
- 친구 검색, 길게 누르기 메뉴, 정보 변경, 삭제가 서로 충돌하지 않는지 확인한다.
- 방 생성 직후 제목이 친구 이름 또는 그룹명 규칙대로 표시되는지 다시 점검한다.
- 우측 스크롤 표시가 목록 길이에 따라 모든 주요 리스트에서 일관되게 동작하는지 확인한다.
- `localStorage` 초기화, 비어 있는 상태, 복수 방, 복수 친구 상태를 각각 점검한다.

## 정리

- `assets/js/app.js`의 남은 레거시 분기와 중복 로직을 더 줄인다.
- `WORKLOG.md`와 `TODO.md`를 날짜별로 계속 갱신할 수 있게 형식을 유지한다.
- 필요하면 친구, 채팅, 홈 화면별로 중복되는 DOM 조작을 별도 공통 유틸로 분리한다.
